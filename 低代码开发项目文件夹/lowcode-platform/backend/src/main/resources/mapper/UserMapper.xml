<?xml version="1.0" encoding="UTF-8"?>
<!-- UserMapper.xml 临时禁用
     此文件已被临时注释以避免 MyBatis Plus 兼容性问题
     等兼容性问题解决后将恢复此映射文件的功能
-->
<!--
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lowcode.mapper.UserMapper">
    
    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" parameterType="string" resultType="com.lowcode.entity.User">
        SELECT * FROM sys_user 
        WHERE username = #{username} AND deleted = 0
    </select>
    
    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" parameterType="string" resultType="com.lowcode.entity.User">
        SELECT * FROM sys_user 
        WHERE email = #{email} AND deleted = 0
    </select>
    
    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" parameterType="string" resultType="com.lowcode.entity.User">
        SELECT * FROM sys_user 
        WHERE phone = #{phone} AND deleted = 0
    </select>
    
    <!-- 更新最后登录信息 -->
    <update id="updateLastLogin">
        UPDATE sys_user 
        SET last_login_time = NOW(), 
            last_login_ip = #{loginIp}
        WHERE id = #{userId} AND deleted = 0
    </update>
    
</mapper>
-->